# Database
DATABASE_URL="postgresql://medizo:medizo_dev@localhost:5432/medizo?schema=public"
POSTGRES_PASSWORD="medizo_dev"

# NextAuth
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"
AUTH_TRUST_HOST="true"

# Image uploads (local filesystem, mounted volume in docker-compose)
UPLOAD_DIR="./storage/uploads"

# Google Genkit (optional, only if using AI flows)
GOOGLE_API_KEY=""
